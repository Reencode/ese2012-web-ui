%link(rel="stylesheet" type="text/css" href="../style.css")
%h1

%h2 Do you really want to buy this item?
%p
 #{itemToBuy.to_s}
 %p


  Your credit now: #{Users::User.by_name(current_name).amount}
  Your credit after the deal: #{Users::User.by_name(current_name).amount - itemToBuy.price}

 %form( action="/buy/#{itemToBuy.id}" method="POST" )
  %input{:type => "submit", :value =>"Yes", :name => "Yes", :class => "button"}
 %form( action="/" method="GET" )
  %input{:type => "submit", :value =>"Abort", :name => "Abort", :class => "button"}

%a{:href => "/logout"} Logout
%a{:href => "/"} Back

%hr
Time: #{time.strftime("%H:%M:%S")}